- hosts: all
  tasks:
    - name: Ensure 'Access Credential Manager as a trusted caller' is set to 'No One'
      win_regedit:
        path: HKEY_LOCAL_MACHINE\SOFTWARE\Microsoft\Windows\CurrentVersion\Policies\CredentialManager
        name: AllowOnlyNoPassword
        data: 0
        type: dword

---
- name: Configurar la auditoría para 'Audit Account Lockout' para 'Failure'
  hosts: all
  gather_facts: no

  tasks:
    - name: Verificar la configuración actual para 'Audit Account Lockout'
      win_shell: |
        $currentStatus = auditpol /get /subcategory:"Account Lockout" | Select-String -Pattern "Failure"
        if (!$currentStatus) {
          auditpol /set /subcategory:"Account Lockout" /failure:enable
          Write-Output "Configuración de auditoría ajustada a 'Failure'"
        } else {
          Write-Output "La auditoría ya está configurada para 'Failure'"
        }
      register: audit_result

    - name: Confirmar que la configuración se realizó correctamente
      debug:
        msg: "{{ audit_result.stdout }}"
